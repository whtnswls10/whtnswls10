// MathVerse - 로컬 정적 프리뷰(file://) 환경 설정 파일
// 이 파일을 복사하여 'config.js' 로 이름을 변경한 후 아래에 OpenAI API 키를 입력하면
// 로컬 파일 더블클릭 실행 시에도 별도 입력 없이 AI 챗봇이 자동으로 동작합니다.
// (주의: config.js 파일은 .gitignore에 등록되어 깃허브에 올라가지 않습니다.)

window.MATHVERSE_CONFIG = {
  // OpenAI API 키 (sk-...)
  OPENAI_API_KEY: ""
};
